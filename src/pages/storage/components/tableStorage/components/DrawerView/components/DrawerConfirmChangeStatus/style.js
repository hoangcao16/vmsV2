import MSCustomizeDrawer from '@/components/Drawer';
import styled from 'styled-components';

export const MSCustomizeDrawerStyled = styled(MSCustomizeDrawer)`
  .confirmChange-header {
    margin-top: 60px;
    font-weight: 500;
    font-size: 24px;
    text-align: center;
  }
`;
